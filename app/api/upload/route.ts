import { getWorkerEnv, requireAdmin } from "@/lib/store";

const allowed = new Map([["image/jpeg","jpg"],["image/png","png"],["image/webp","webp"]]);
export async function POST(request:Request){const auth=await requireAdmin(request);if(!auth.ok)return Response.json({error:auth.error},{status:auth.status});try{const env=await getWorkerEnv();if(!env.MEDIA)return Response.json({error:"MEDIA R2 binding is not configured."},{status:503});const data=await request.formData(),file=data.get("file");if(!(file instanceof File))return Response.json({error:"Choose an image."},{status:400});const ext=allowed.get(file.type);if(!ext)return Response.json({error:"Only JPG, PNG and WebP are allowed."},{status:400});if(file.size>8*1024*1024)return Response.json({error:"Image must be under 8 MB."},{status:400});const key=`${Date.now()}-${crypto.randomUUID().slice(0,12)}.${ext}`;await env.MEDIA.put(key,await file.arrayBuffer(),{httpMetadata:{contentType:file.type}});return Response.json({ok:true,url:`/api/media/${key}`})}catch(e){return Response.json({error:e instanceof Error?e.message:"Upload failed."},{status:500})}}
