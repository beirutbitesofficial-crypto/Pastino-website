import { getWorkerEnv } from "@/lib/store";
export async function GET(_request:Request,context:{params:Promise<{key:string}>}){const env=await getWorkerEnv();if(!env.MEDIA)return new Response("Media storage is not configured.",{status:503});const {key}=await context.params;if(!/^[a-zA-Z0-9._-]+$/.test(key))return new Response("Invalid key.",{status:400});const object=await env.MEDIA.get(key);if(!object)return new Response("Not found.",{status:404});const headers=new Headers({"Cache-Control":"public, max-age=31536000, immutable"});object.writeHttpMetadata?.(headers);if(object.httpEtag)headers.set("ETag",object.httpEtag);return new Response(object.body,{headers})}
